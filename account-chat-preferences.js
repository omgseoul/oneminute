window.AccountChatPreferences=(()=>{
 let state,auth,loading;
 const esc=s=>GuestSupport.esc(s);
 async function mount(token){auth={access_token:token};if(!state){if(!loading)loading=GuestSupport.call('settings',{},auth).then(v=>state=v).finally(()=>loading=null);await loading;}
 document.querySelectorAll('.account-row').forEach(row=>{if(row.querySelector('.chat-pref'))return;const key=row.dataset.employeeId?'employee:'+row.dataset.employeeId:row.dataset.ownerId?'owner:'+row.dataset.ownerId:null;if(!key)return;const account=state.accounts.find(a=>a.actor_key===key);if(!account)return;const p=account.preferences||{},el=document.createElement('details');el.className='chat-pref';el.innerHTML=`<summary>게스트 메세지 알림 설정 <span>${p.enabled?'사용 중':'꺼짐'}</span></summary><label class="cp-check"><input class="cp-enabled" type="checkbox" ${p.enabled?'checked':''}>게스트 메세지 알림 받기</label><div class="cp-options" ${p.enabled?'':'hidden'}><p>알림 받을 지점</p><div class="cp-properties">${state.properties.map(v=>`<label class="cp-check"><input class="cp-property" type="checkbox" value="${v.id}" ${(p.property_ids||state.properties.filter(x=>x.own).map(x=>x.id)).includes(v.id)?'checked':''}>${esc(v.name)}</label>`).join('')}</div><p>알림 요일</p><div class="cp-days">${['일','월','화','수','목','금','토'].map((d,i)=>`<label><input class="cp-day" type="checkbox" value="${i}" ${(p.days||[0,1,2,3,4,5,6]).includes(i)?'checked':''}>${d}</label>`).join('')}</div><div class="cp-times"><label>시작<input class="cp-start" type="time" value="${esc((p.start_time||'').slice(0,5))}"></label><label>종료<input class="cp-end" type="time" value="${esc((p.end_time||'').slice(0,5))}"></label></div><small>시간을 모두 비우면 종일 · 소속 숙소 시간 기준</small></div><div class="cp-footer"><small class="cp-status" role="status"></small><button type="button" class="cp-save">알림 설정 저장</button></div>`;
 el.querySelector('.cp-enabled').onchange=e=>el.querySelector('.cp-options').hidden=!e.target.checked;
 el.querySelector('.cp-save').onclick=async()=>{const btn=el.querySelector('.cp-save'),status=el.querySelector('.cp-status');btn.disabled=true;try{const next={actor_key:key,enabled:el.querySelector('.cp-enabled').checked,property_ids:[...el.querySelectorAll('.cp-property:checked')].map(x=>x.value),days:[...el.querySelectorAll('.cp-day:checked')].map(x=>+x.value),start_time:el.querySelector('.cp-start').value,end_time:el.querySelector('.cp-end').value};if(next.enabled&&!next.property_ids.length)throw new Error('지점을 선택해주세요.');if(!next.days.length)throw new Error('요일을 선택해주세요.');if(!!next.start_time!==!!next.end_time)throw new Error('시작·종료 시간을 모두 입력해주세요.');await GuestSupport.call('save_preferences',{preferences:[next]},auth);account.preferences=next;el.querySelector('summary span').textContent=next.enabled?'사용 중':'꺼짐';status.textContent='저장했습니다.';}catch(e){status.textContent=e.message;}finally{btn.disabled=false;}};row.append(el);
 });}
 return {mount};
})();
